# [PRD] 부산 양정고등학교 급식 정보 조회 서비스 (Yangjeong High School Meal Service)

---

## 1. 제품 개요 (Product Overview)
- **제품명**: 부산 양정고등학교 스마트 급식 정보 조회 서비스
- **목적**: 교육부 나이스(NEIS) 오픈 API를 실시간으로 연동하여 부산 양정고등학교 학생, 학부모, 교직원이 중식 및 석식, 알레르기 유발 식품, 영양 성분, 칼로리, 원산지 정보를 직관적이고 쾌적하게 확인할 수 있도록 돕는 웹 애플리케이션
- **핵심 특징**:
  1. **3D 드럼 휠 데이트 피커**: 정밀한 스프링 물리 엔진과 Web Audio API 기반의 기계식 틱 사운드가 결합된 인터랙티브 날짜 선택기
  2. **실시간 NEIS OpenAPI 자동 동기화**: 수동 입력 없이 공식 나이스 교육행정정보 개방포털에서 중식·석식·조식 데이터를 실시간 파싱
  3. **알레르기 안심 필터**: 19종 법정 알레르기 유발 물질 맞춤 설정 및 메뉴별 즉각적인 시각 경고
  4. **주간 급식표 종합 뷰**: 월~금 5일간의 중식/석식을 자유롭게 전환하며 비교하고 클립보드로 간편 복사
  5. **벤토 그리드(Bento Grid) & 다크 모드**: 세련된 카드 레이아웃과 완벽한 다크/라이트 테마 지원

---

## 2. 타깃 사용자 (Target Audience) 및 페르소나

| 사용자 그룹 | 주요 니즈 및 사용 시나리오 |
| :--- | :--- |
| **양정고 재학생** | • 오늘/내일/이번 주 점심(중식) 및 야자 저녁(석식) 메뉴를 빠르게 확인<br>• 특정 알레르기가 있어 위험 메뉴를 사전에 필터링<br>• 주간 식단표를 확인하고 친구들과 메신저로 메뉴를 공유 |
| **학부모** | • 자녀가 학교에서 섭취하는 식단의 영양 성분, 칼로리, 원산지 정보 확인<br>• 주간 식단을 미리 파악하여 가정 내 식단 계획에 반영 |
| **교직원 및 영양사** | • 공식 NEIS 급식 정보 연동 상태 점검<br>• 주간/일별 급식 데이터 복사 및 공유 |

---

## 3. 핵심 기능 요구사항 (Functional Requirements)

### 3.1. NEIS OpenAPI 실시간 연동 및 데이터 파싱
- **학교 식별 정보**:
  - 시도교육청코드 (`ATPT_OFCDC_SC_CODE`): `C10` (부산광역시교육청)
  - 표준학교코드 (`SD_SCHUL_CODE`): `7150086` (양정고등학교)
- **데이터 파싱 엔진**:
  - `DDISH_NM`: HTML 태그(`<br/>` 등) 및 인코딩 엔티티를 정제하여 줄바꿈 단위로 분리
  - 알레르기 번호 추출: `(1.2.5.6)` 형태의 번호 표기를 감지하여 메뉴명과 알레르기 코드로 분리
  - 식사 구분 (`MMEAL_SC_CODE`): `1`(조식), `2`(중식), `3`(석식)을 자동 분류하여 정렬
  - 영양 정보(`NTR_INFO`), 칼로리(`CAL_INFO`), 원산지 정보(`ORPLC_INFO`) 파싱 및 시각화
- **인메모리 캐싱**:
  - 이미 조회된 날짜 데이터는 클라이언트 캐시에 보관하여 중복 네트워크 요청 방지 및 즉각적인 UI 응답성 보장

### 3.2. 3D 스프링 드럼 휠 날짜 선택기 (Spring Date Picker)
- **3개 독립 컬럼 구성**: 연도(Year), 월(Month), 일(Day) 롤러 휠
- **물리 인터랙션**:
  - 마우스 드래그, 터치 스와이프, 마우스 휠 스크롤 지원
  - 운동량(Velocity) 및 스프링 감쇠(Damping)를 적용하여 쫀득하고 자연스러운 스냅 애니메이션 제공
- **햅틱 사운드 피드백**:
  - Web Audio API를 활용하여 휠이 돌아갈 때 기계식 조그 다이얼 틱 사운드 재생 (ON/OFF 가능)
- **날짜 정합성 보장**:
  - 윤년(Leap Year) 및 각 월의 말일(28, 29, 30, 31일)을 동적으로 계산하여 유효하지 않은 일자 자동 보정
- **선택된 날짜 강조**:
  - 롤러 중앙의 포커스 밴드 및 상단 상태창에 선택 날짜를 선명한 블루 텍스트와 굵은 서체로 표기
- **원클릭 빠른 이동(Quick Jump)**:
  - `오늘`, `내일`, `어제` 버튼 제공
  - `다음 급식일 자동 탐색`: 주말/공휴일 등 급식이 없는 날짜에서 클릭 시, 이후 최대 30일 내 가장 가까운 급식 운영일을 자동 탐색하여 즉시 이동

### 3.3. 일별 급식 카드 (Meal Card)
- **중식 / 석식 탭 전환**:
  - 당일 중식과 석식이 모두 제공되는 경우 탭 버튼(☀️ 중식 / 🌙 석식)을 통해 즉시 전환
- **메뉴 리스트 줄바꿈 및 불릿 표기**:
  - 모든 메뉴 항목은 각 1줄씩 명확하게 배치되며, 메뉴별 상세 알레르기 뱃지 제공
- **알레르기 위험 강조**:
  - 사용자가 설정한 알레르기 유발 물질이 포함된 요리는 붉은색 테두리, 배경 하이라이트 및 경고 아이콘 표시
- **부가 정보 탭**:
  - `칼로리 및 영양 정보`: 탄수화물, 단백질, 지방, 비타민 등 상세 성분 테이블
  - `식재료 원산지`: 쌀, 김치, 육류 등 품목별 원산지 표기
- **원클릭 텍스트 복사**:
  - 당일 식단 목록, 열량, 출처를 포맷팅된 텍스트로 클립보드에 복사

### 3.4. 주간 급식표 모달 (Weekly Meal Modal)
- **5일(월~금) 벤토 그리드 레이아웃**:
  - 선택한 주차의 월요일부터 금요일까지 5일간의 식단을 카드 형태로 한눈에 표시
- **중식 / 석식 / 조식 / 전체 탭 전환**:
  - 주간 중식 및 석식을 버튼 하나로 전환하여 주간 전체를 비교 조회
  - NEIS OpenAPI를 통해 실시간 데이터를 수신하여 수동 기입 없이 자동 표출
- **주간 이전/다음 주 이동**:
  - `이전 주`, `이번 주`, `다음 주` 네비게이션 버튼 제공
- **주간 식단 전체 복사**:
  - 주간 5일간의 식단을 깔끔한 포맷의 텍스트로 일괄 복사하여 메신저 등에 공유 가능
- **일자 바로가기**:
  - 주간 카드에서 `이 날짜로 이동` 클릭 시 메인 데이트 피커 및 화면이 해당 일자와 식사 구분으로 즉시 동기화

### 3.5. 19종 법정 알레르기 필터링 시스템 (Allergy Filter Modal)
- **19개 알레르기 식품 관리**:
  1. 난류(가금류) / 2. 우유 / 3. 메밀 / 4. 땅콩 / 5. 대두 / 6. 밀 / 7. 고등어 / 8. 게 / 9. 새우 / 10. 돼지고기 / 11. 복숭아 / 12. 토마토 / 13. 아황산류 / 14. 호두 / 15. 닭고기 / 16. 쇠고기 / 17. 오징어 / 18. 조개류(굴·전복·홍합 포함) / 19. 잣
- **개인화 설정 저장**:
  - 사용자가 선택한 알레르기 항목은 `localStorage`에 자동 저장되어 재방문 시에도 유지

### 3.6. 테마 및 사운드 환경설정
- **다크 모드 / 라이트 모드**:
  - 사용자의 시스템 설정 자동 감지 및 헤더 토글 버튼으로 즉시 전환
- **효과음 토글**:
  - 드럼 휠 사운드 ON/OFF 제어

---

## 4. 비기능적 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 기준 |
| :--- | :--- |
| **반응형 디자인 (Responsiveness)** | • 모바일(320px~), 태블릿(768px~), 데스크톱(1024px~) 전 해상도 대응<br>• 모바일 환경에서 터치 제스처 및 44px 이상의 터치 타깃 준수 |
| **성능 및 반응속도** | • 날짜 변경 시 인메모리 캐싱을 통해 0.05초 이내 즉각적인 렌더링<br>• 초기 페이지 로딩 속도 1.5초 이내 완료 |
| **안정성 및 예외 처리** | • NEIS API 장애, 주말/방학/공휴일 등 급식 미운영 시 친절한 Empty State 및 대체 안내 제공<br>• 자동 재시도 버튼 및 다음 급식일 자동 탐색 기능 제공 |
| **접근성 및 가독성 (a11y)** | • WCAG AA 기준(4.5:1 이상)의 텍스트 대비 준수<br>• 주요 상호작용 요소에 고유 `id` 및 `aria-label` 속성 부여 |
| **데이터 보안 및 무결성** | • 클라이언트 사이드에서 불필요한 민감 정보 수집 없음<br>• 설정값(테마, 사운드, 알레르기)은 브라우저 로컬 스토리지에 안전하게 격리 저장 |

---

## 5. 시스템 아키텍처 및 데이터 흐름

```
[사용자 인터랙션 (드럼 휠/탭/모달)]
             │
             ▼
[React State (selectedDate, userAllergies, preferredMeal)]
             │
     ┌───────┴───────┐
     ▼               ▼
[인메모리 캐시] ──(Miss 시)──► [NEIS Open API (open.neis.go.kr)]
     │                                │
     │ (Hit 시 즉시 반환)              ▼
     │                       [convertRowToParsedMeal]
     │                       - DDISH_NM 줄바꿈 & 불릿 분리
     │                       - (1.2.5.6) 알레르기 정규식 추출
     │                       - 칼로리 / 영양소 / 원산지 파싱
     ▼                                │
[ParsedMeal[] 데이터 구조화] ◄─────────┘
     │
     ▼
[UI 컴포넌트 렌더링]
 ├── Header (테마/사운드/모달 트리거)
 ├── SpringDatePicker (3D 드럼 휠 + 햅틱 사운드)
 ├── MealCard (중식/석식 탭, 알레르기 하이라이트, 상세정보)
 ├── WeeklyMealModal (주간 식단 그리드 & 중/석식 토글)
 └── AllergyFilterModal (19종 알레르기 맞춤 선택)
```

---

## 6. 데이터 모델 규격 (TypeScript Interfaces)

```typescript
// 요리 항목
export interface DishItem {
  cleanName: string;         // 알레르기 번호가 제거된 순수 요리명
  allergyCodes: number[];    // 해당 요리에 포함된 알레르기 코드 배열 (예: [1, 5, 6])
  hasUserAllergy?: boolean;  // 사용자 등록 알레르기 포함 여부
}

// 식단 정보
export interface ParsedMeal {
  schoolName: string;        // 학교명 (양정고등학교)
  dateString: string;        // YYYYMMDD
  formattedDate: string;     // YYYY년 M월 D일 (요일)
  mealCode: string;          // 1: 조식, 2: 중식, 3: 석식
  mealName: string;          // '중식', '석식', '조식'
  dishes: DishItem[];        // 요리 리스트
  calories: string;          // 칼로리 (예: '850.4 Kcal')
  nutritionInfo: string[];   // 영양 성분 리스트
  originInfo: string[];      // 원산지 정보 리스트
}

// 날짜 선택 상태
export interface DateSelection {
  year: number;
  month: number;
  day: number;
}
```

---

## 7. UI/UX 디자인 원칙 (Bento Style & Micro-Interactions)
1. **Zero Cliché (안티-슬롭 원칙)**: 불필요한 보라-파랑 그라디언트나 과도한 그림자를 배제하고, 차분한 슬레이트 뉴트럴과 선명한 블루 액센트를 조화롭게 적용
2. **시각적 위계**: 선택된 날짜와 메인 요리명을 굵고 큰 타이포그래피(24px+, Black)로 배치하여 한눈에 파악 가능
3. **손쉬운 조작감**: 휠 스크롤, 드래그, 퀵 점프 버튼 등 다채로운 입력 방식을 지원하여 어떤 디바이스에서도 쾌적한 사용감 선사
4. **가독성 최우선**: 모든 식단 항목은 1줄 1메뉴 불릿 구조로 정렬하여 가독성을 극대화

---

## 8. 향후 로드맵 (Future Roadmap)
- [ ] 오늘/내일 급식 카카오톡/라인/디스코드 웹훅 알림 기능
- [ ] 학생 급식 선호도 투표 및 베스트 메뉴 랭킹 기능
- [ ] 학사일정(시험, 방학, 개교기념일 등) 캘린더 연동 뷰
